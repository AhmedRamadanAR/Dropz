name: Dev Branch Checks

on:
  pull_request:
    branches: [dev]
  push:
    branches: [dev]

jobs:
  backend:
    name: Django Backend Lint + Test
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: dropz_db
          POSTGRES_USER: dropz_user
          POSTGRES_PASSWORD: ${{ secrets.DB_PASSWORD }}
        ports: ['5432:5432']
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      DB_NAME: dropz_db
      DB_USER: dropz_user
      DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
      DB_HOST: localhost
      DB_PORT: 5432
      SECRET_KEY: ${{ secrets.SECRET_KEY }}
      DEBUG: "False"
      ALLOWED_HOSTS: localhost,127.0.0.1

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.12"

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential libpq-dev curl

    - name: Install Python dependencies
      working-directory: backend
      run: |
        pip install -r requirements.txt
        pip install flake8 black pytest

    - name: Run flake8 linter
      working-directory: backend
      run: flake8 .

    - name: Run black formatter check
      working-directory: backend
      run: black --check .

    - name: Run Django tests
      working-directory: backend
      run: pytest
      env:
        DJANGO_SETTINGS_MODULE: core.settings

  frontend:
    name: React Frontend Lint + Build
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Node
      uses: actions/setup-node@v4
      with:
        node-version: 20

    - name: Install frontend dependencies
      working-directory: frontend
      run: npm install

    - name: Run ESLint
      working-directory: frontend
      run: npx eslint . --ext .js --ext .jsx --ext .ts --ext .tsx

    - name: Build React app
      working-directory: frontend
      run: npm run build
